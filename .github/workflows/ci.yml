name: ipty build test

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: daisukebtw
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Build Docker image
        run: |
          docker build -t daisukebtw/$API_IMAGE_NAME ./api
          docker build -t daiuskebtw/$APP_IMAGE_NAME ./app
      
      - name: Push Docker image
        run: |
          docker push daisukebtw/$API_IMAGE_NAME
          docker push daisukebtw/$APP_IMAGE_NAME


      - name: Checkout configuration repository
        uses: actions/checkout@v4
        with:
          repository: ${{ secrets.K8S_CONFIG_REPO }}
          token: ${{ secrets.K8S_CONFIG_REPO_TOKEN }}
          ref: main

      - name: Update API and APP Kubernetes manifest
        run: |
          API_MANIFEST_FILE=api-ipty-deployment.yaml
          APP_MANIFEST_FILE=app-ipty-deployment.yaml
          sed -i "s|image: .*|image: $API_IMAGE_NAME|" $API_MANIFEST_FILE
          sed -i "s|image: .*|image: $APP_IMAGE_NAME|" $APP_MANIFEST_FILE
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add $API_MANIFEST_FILE $APP_MANIFEST_FILE
          git commit -m "Update Docker image versions"

      - name: Push changes
        run: git push


